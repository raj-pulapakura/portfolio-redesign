import { Inter, Rock_Salt, Noto_Sans } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const rockSalt = Rock_Salt({ weight: '400', subsets: ['latin'] });

export const notoSans = Noto_Sans({ subsets: ['latin'] });
